% azure, cli

# List all accounts
az account list

# Change (Switch) an accounts
az account set

# List all locations (regions)
az account list-locations

# Create virtual machine(s)
az vm create

# List the VM/VMSS images available in the Azure Marketplace
az vm image list
